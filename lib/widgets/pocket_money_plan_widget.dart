import 'package:flutter/material.dart';

class PocketMoneyPlanWidget extends StatelessWidget {
  const PocketMoneyPlanWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
