import 'package:flutter/material.dart';

class PocketMoneyPlanDetailWidget extends StatelessWidget {
  const PocketMoneyPlanDetailWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
